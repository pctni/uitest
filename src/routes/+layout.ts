export const prerender = true;
export const ssr = false;
// This is a client-side only application, so we can safely disable SSR.
// If you need SSR, you'll need to adjust the MapLibre and PMTiles initialization.
